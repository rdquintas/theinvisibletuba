{
  "name": "The Invisible Tuba",
  "version": "0.1.0",
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-bake": "^1.9.0",
    "grunt-contrib-watch": "^1.1.0"
  },
  "scripts": {
    "git-deploy": "git add . && git commit -m 'fixes' && git push & git ftp push"  
  }
}
